[project]
name = "agri-integrity-mesh"
authors = []
description = ""
telemetry = true
requirements = []
cache_dir = "/home/runner/workspace/agri-integrity-mesh/./.requirements"
boot_contracts = ["pox", "costs-v2", "bns"]
[contracts.agri-integrity-mesh]
path = "contracts/agri-integrity-mesh.clar"

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
